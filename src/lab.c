#include "../inc/header.h"
#include <stdio.h>

/*void print_lab(s_point **map, int size_x, int size_y){
  for (int i = 0; i < size_x; i++) {
    for (int j = 0; j < size_y; j++) {
      printf("%d", map[i][j].is_wall);
    }
    printf("\n");
  }  
  }*/
